'use client'
import { useAuth } from '@/hooks/useAuth'

const DashboardPage = () => {
  
  return (
    <div className="pt-12">
      {`You're logged in!`}
    </div>
  )
}

export default DashboardPage
